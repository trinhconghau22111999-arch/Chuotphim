<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <!-- Chỉ cần trên Android 9-11 (API 28-30): trước khi có cờ "neverForLocation"
         ở BLUETOOTH_SCAN (Android 12+), hệ thống bắt buộc phải có quyền vị trí
         thì startDiscovery()/ACTION_FOUND mới trả kết quả quét Bluetooth, dù app
         không hề dùng để định vị GPS thật — chỉ dùng RSSI để so sánh gần/xa
         tương đối giữa các thiết bị đã pair (xem ScanDevicesDialog,
         ProximityAutoConnector). Từ Android 12 trở lên không cần quyền này nữa
         nên giới hạn maxSdkVersion=30. -->
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-feature android:name="android.hardware.bluetooth" android:required="true" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Android 11 (API 30) trở lên giới hạn "package visibility": nếu KHÔNG khai
         báo <queries> này, có thể SpeechRecognizer.isRecognitionAvailable() trả về
         false hoặc startListening() không hoạt động trên 1 số máy dù đã có quyền
         RECORD_AUDIO, vì hệ thống không cho app "nhìn thấy" dịch vụ nhận diện
         giọng nói đang cài trên máy. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
